# OpenSpec Configuration — wireguard-vpn-client
# Auto-generated by sdd-init on 2026-07-06
# Mode: openspec

project:
  name: wireguard-vpn-client
  description: A desktop WireGuard VPN client with cloud auto-provisioning
  version: 0.1.0

stack:
  frontend: Svelte 5 + TypeScript + Vite 6
  desktop: Tauri v2
  backend: Rust 2021 edition (Tokio async)
  languages:
    - TypeScript (frontend)
    - Rust (backend)

architecture:
  pattern: Modular clean separation
  modules:
    - cloud/ — Cloud provider integrations (DO, Hetzner, Oracle)
    - config/ — AppConfig JSON persistence + secrets (AES-256-GCM/keyring)
    - i18n/ — Internationalization (en/es)
    - provision/ — SSH server provisioning
    - tunnel/ — WireGuard tunnel engine (system/embedded)
    - vpn/ — Connection state machine
  notes:
    - Svelte components provide the UI layer
    - Tauri commands bridge Rust <-> TypeScript
    - Mutex<State> pattern for shared app state in lib.rs

strict_tdd: true
strict_tdd_note: >
  Rust test runner (cargo test) is available with existing tests in config,
  secrets, and i18n modules. No JS/TS test runner is configured — strict TDD
  applies to Rust backend only. Frontend testing setup (vitest or Playwright)
  is recommended for Phase 7-8.

testing:
  runner:
    rust: cargo test
    js: none available
  layers:
    unit: Available (Rust #[cfg(test)])
    integration: Not configured (no tests/ directory at Rust level)
    e2e: Not configured
  coverage:
    available: false
    note: Add cargo-tarpaulin or cargo-llvm-cov for Rust coverage
  quality:
    linter:
      rust: clippy (available via cargo clippy)
      js: svelte-check (available via npx svelte-check)
    type_checker:
      rust: cargo check
      js: tsc (via tsconfig.json, strict mode)
    formatter:
      rust: rustfmt (available via cargo fmt)
      js: not configured

phases:
  proposal: true
  spec: true
  design: true
  tasks: true
  apply: true
  verify: true
  archive: true

onboarding_markers:
  - openspec/config.yaml
  - openspec/changes/wireguard-vpn-client/tasks.md
  - .atl/skill-registry.md
